# src/

Alchemy Lab — жанровая адаптация template под **merge + discovery + orders**.

## Что добавлено
- `core/alchemy.ts` — core loop лаборатории
- `ui/alchemy.ts` — экран алхимического стола
- order board, journal, catalyst и grid pressure

## Команды

```bash
npm install
npm run typecheck
npm run build
npm run dev
```
